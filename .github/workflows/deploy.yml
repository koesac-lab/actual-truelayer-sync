name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Sync repo to deploy path
        run: |
          DEPLOY_DIR=/home/koesac/apps/actual-truelayer-sync
          mkdir -p "$DEPLOY_DIR"
          rsync -a --delete \
            --exclude '.git/' \
            "$GITHUB_WORKSPACE"/ "$DEPLOY_DIR"/

      - name: Build setup image
        working-directory: /home/koesac/apps/actual-truelayer-sync
        run: docker compose build actual-truelayer-setup

      - name: Clean up old images
        run: docker image prune -f
